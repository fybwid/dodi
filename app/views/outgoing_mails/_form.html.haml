= form_with(model: outgoing_mail, local: true) do |form|
  - if outgoing_mail.errors.any?
    #error_explanation
      %h2
        = pluralize(outgoing_mail.errors.count, "error")
        prohibited this outgoing_mail from being saved:
      %ul
        - outgoing_mail.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
