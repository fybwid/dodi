= form_with(model: incoming_mail, local: true) do |form|
  - if incoming_mail.errors.any?
    #error_explanation
      %h2
        = pluralize(incoming_mail.errors.count, "error")
        prohibited this incoming_mail from being saved:
      %ul
        - incoming_mail.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit